language: node_js
cache:
    yarn: true
script:
    - yarn validate
    - yarn test:unit
    - yarn build
    - yarn test:regression
deploy:
    skip_cleanup: true
    provider: firebase
    token: $FIREBASE_TOKEN
